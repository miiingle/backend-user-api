/**
 * business logic, responsible for coordinating persistence and other services
 * this layer should be pure from the concrete implementations of external services
 * as well as the existence of the presentation layer.
 *
 * -if we retain only the "business" package, this should still compile
 */
package net.miiingle.user.api.business;