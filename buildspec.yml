version: 0.2
        
phases:
  install:
    runtime-versions:
      java: corretto11
    commands:
      - java --version
  build:
    commands:
      - ./gradlew build
      - ./gradlew dockerBuild
      - docker images